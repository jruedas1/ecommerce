<h2>Thanks for shopping with us!</h2>
<p>Let's review your order:</p>
<!-- k means key, v means value -->
<!-- this loops through the order items hash -->
<!-- k is product id, v is quantity -->
<% @order.order_items.each do |k,v| %>
  <ul>
    <li><%= Product.find(k).brand %> <%= Product.find(k).name %>, <%= v %></li>
  </ul>
<% end %>

<p>
  <strong>Order Total:</strong> <%= number_to_currency @order.grand_total %>
</p>